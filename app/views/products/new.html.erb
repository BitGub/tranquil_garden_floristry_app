
<%= link_to '< Back', products_path %>

<%= form_with(model: @product,  local:true, html: { class: "needs-validation", novalidate:true } )  do |product| %>

  <%= render 'fields', f: product %>

<% end %>